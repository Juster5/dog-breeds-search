import React from "react"
import "./index.scss"

// type LoadingProps = {
//   size?: "sm" | "mid" | "bg"
// }

const Loading = () => {
  return <div className={`s-loading`}></div>
}

export default React.memo(Loading)
